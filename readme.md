laravel - vue - mongodb  

feature : 
* laravel auth with mongodb 
* vue router single page application

setup
    
* copy ```.env.example``` to ```.env```
* run ```composer update```
* run ```npm install ```

development run

* ```npm run watch```
* ```php artisan serve```





